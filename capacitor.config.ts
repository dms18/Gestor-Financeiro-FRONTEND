import { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: "com.diegomarques.gestorfianceiro",
  appName: "Gestor Financeiro",
  bundledWebRuntime: false,
  npmClient: "npm",
  webDir: "www",
};

export default config;
